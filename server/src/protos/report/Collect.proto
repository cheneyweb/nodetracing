syntax = "proto3";
package report;

// 服务定义
service Collect {
  rpc upload (ReportReq) returns (ReportRes) {}
}

// 请求返回定义
message ReportReq {
  string spans = 1;   // json
}

message ReportRes {
  string code = 1;
  string res = 2;
}